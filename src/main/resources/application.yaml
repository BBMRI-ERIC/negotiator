server:
  port: 8080
  servlet:
    context-path: "/api"
spring:
  application:
    name: "Negotiator"
  jackson:
    date-format: "yyyy-MM-dd"
  jpa:
    open-in-view: true
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri:
negotiator:
  redirectPath: "/gui/negotiation"
  authorization:
    claim: ""
    adminClaimValue: ""
    researcherClaimValue: ""
    biobankerClaimValue: ""
    subjectClaim: ""
logging:
  level:
    org:
      springframework:
        jdbc:
          datasource:
            init:
              ScriptUtils: debug

